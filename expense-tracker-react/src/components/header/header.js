import React from "react";
import "./header.scss";

const Header = (props) => {
  return (
    <div className="component-header">
      <h2>Expense Tracker</h2>
    </div>
  );
};

export default Header;
